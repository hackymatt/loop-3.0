// next-i18next.config.ts

const config = {
  i18n: {
    defaultLocale: "pl",
    locales: ["pl", "en"],
    localeDetection: true,
  },
  reloadOnPrerender: true,
};

export default { config };
